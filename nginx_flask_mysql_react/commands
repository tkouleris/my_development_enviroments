docker compose exec web sh -c 'flask db init'
docker compose exec web sh -c 'flask db migrate -m "Initial migration."'
docker compose exec web sh -c 'flask db upgrade'

docker compose exec web sh -c "pip freeze > requirements.txt"



docker compose --profile dev up
docker compose --profile prod up

